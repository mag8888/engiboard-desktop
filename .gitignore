# Rust
src-tauri/target/
# NB: Cargo.lock MUST be committed for a binary app — without it CI resolves
# deps fresh and can pull an incompatible transitive crate (e.g. time 0.3.52
# broke cookie 0.18.1, failing every platform's release build on 2026-06-30).

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.iml

# Build output (keep dist/*.html tracked for Railway deployment)
build/

# Dependencies
node_modules/
package-lock.json

# Test artifacts
test-results/

# Local stuff
*.log
.env
.env.local
.env.local
.env*.local
.env
.claude/

# UI test artifacts
playwright-report/
screenshots/
.playwright/
