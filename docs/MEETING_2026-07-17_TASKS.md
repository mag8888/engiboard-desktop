# EngiBoard — задачи со встречи с Дмитрием 2026-07-17

Источник: `Tasks for CAD - 2026_07_17` (запись ~47 мин, локальная транскрипция whisper).
Аудио: `~/Downloads/Tasks_for_CAD_2026-07-17_audio.mp3`.
Цель встречи: ревью текущей сборки + план на «запуск в работу» (пилотное тестирование инженерами).

Договорённость: Aleksey делает **Приоритет 1**, что успеет из **Приоритета 2** — попутно;
затем версия уходит на тестирование «лётчикам-испытателям»; параллельно начинаем AI-часть.
Следующий созвон — пятница через неделю, то же время.

---

## Дмитрий подтвердил РАБОТАЮЩИМ (регресс прошёл)
Эти фиксы он проверил на сборке — работают, трогать не нужно:
- Умная Drive-ссылка (распознаёт только настоящий Drive-URL).
- Перенос задач между неделями (стало лучше); новосозданная неделя работает, задача при переносе не пропадает.
- «All projects» вернули.
- Проект держится при переходе на Dashboard и обратно («теперь сохраняет»).
- Презентация: растягивается, сообщение чата сохраняется, чат тянется.
- «Добавить инженера» при создании проекта.
- Compare-режим убран.
- Под фильтром имя недели не пропадает.

---

## ПРИОРИТЕT 1 (баги + UI до «запуска в работу»)

### Баги
1. **Захват экрана работает только на ОДНОМ мониторе.** Критично: инженеры работают на 2+ мониторах (приложение на одном, чертёж/трекер на другом). Оверлей захвата смещён и не покрывает второй монитор.
2. **Создание проекта с Dashboard: проект не появляется + клик по проекту не открывает его.** Нужно принудительно переключать на вкладку «Projects» при выборе проекта из Dashboard. (Часть «не появляется» — уже фикс в v0.1.186 через `flush_repaint`, проверить на новой сборке.)
3. **Группа клиента в пикере проектов сворачивается/закрывается сразу.** Клик по клиенту (напр. «ACME MANUFACTURING») должен разворачивать/сворачивать группу, а не мгновенно закрывать её.
4. **Ресайз строки в list-view нельзя уменьшить обратно.** После растягивания строки скролл пропадает и высоту меньше исходной не вернуть — только растёт. Нужно вернуть возможность ужать обратно (и/или сброс к исходному размеру). Только Windows.
5. **Экспорт в Excel — доработать:**
   - В колонку задачи выгружается только ПЕРВЫЙ заголовок чата, а нужен ВЕСЬ чат (все сообщения — это и есть информация, которую нельзя терять).
   - Картинки «плавают» — должны лежать В ЯЧЕЙКАХ, ячейки под размер картинки.
   - Статус/неделя/номер проекта — ок. Можно добавить чипы статусов.
6. **Экспорт в PDF — переделать по аналогии с Excel** (сейчас «всё плохо»).
7. **Презентация: «+Add screenshot» — непонятно, в какую колонку вставит.** Сделать отдельный «+» на КАЖДЫЙ слот (before/after/доп), чтобы не гадать, куда добавится.

### Решение (нужно слово Aleksey)
8. **Комментарии (пины) в редакторе — Дмитрий склоняется УБРАТЬ.** «Не нравятся, самый лёгкий способ — убрать». Противоречит прошлому (мы их чинили на сохранение). Решить: оставляем/убираем.

### UI-улучшения (нужны до запуска)
9. **Стрелки ↑/↓ и drag-ручку (⋮⋮) сделать крупнее.**
10. **Кнопки «Presentation»/«Link» перенести наверх карточки; освободить место под чат/инфо.** Заметки — укоротить в одно компактное поле.
11. **Неделя-бар: поднять контраст + «папочный» отступ.** Содержимое недели визуально сдвигать вправо (как папки в Windows/select-режиме), чтобы было понятно, что задачи ВНУТРИ недели. Показать этот вид по умолчанию.
12. **Dashboard-счётчики считают ПО ПРОЕКТУ.** Как счётчик багов показывает число — так же показывать по статусам, проекция по проекту.
13. **Добавление новой недели — плейсхолдером, как добавление задачи** (постоянный «+ Add week» внизу, по образцу «+ Add task» — Дмитрию зашло).

---

## ПРИОРИТЕT 2 (после P1 / по возможности)

14. **Collapse All / Expand All недель** (сейчас все развёрнуты; стандартная фича).
15. **Undo/Redo на всё приложение** (как в Google Docs), ~10 шагов, все действия.
16. **Presence «кто сейчас в проекте»** (аватарки как в Google Sheets). Aleksey вроде делал — проверить с реальных аккаунтов (кружок на доске).
17. **Чат: редактировать/удалять отдельные сообщения + @упоминания.** Тегнуть человека («@ed …») с уведомлением/отметкой, что тебя тегнули. Механику продумать — «жирная» задача.
18. **Авто-сокращение длинных ссылок в чате** (длинный URL → короткий вид; сейчас просто кликабельны).
19. **Редактор: панель толщин/инструментов сбоку** (как в Paint — под инженеров).
20. **Редактор: текст редактируемый после постановки** (сейчас «печатается» один раз). Спорный — решает команда.
21. **Редактор: инструмент «выноска» — текст со стрелкой (leader line).**
22. **Редактор: multi-line** (полилиния по кликам).
23. **Редактор: Dimension tool** (линия с засечками по бокам + ручной текст, напр. «30 mm»).
24. **Презентация: номер жирным, описание мельче.**
25. **Диалог нового проекта: поле «Language» непонятное** — сделать выпадашкой ИЛИ заменить на «Notes» (assign инженера — оставить).
26. **Метки в редакторе — «печатать» vs «объекты».** Команда спорит; двигать существующие метки после сохранения. Пока НЕ трогаем (решение отложено).
27. **Презентация/карусель доп-скринов — лимит 3 доп (4 всего).** Экспорт多 картинок. Пока «нерелевантно», отложено.

---

## Заметки по процессу (из встречи)
- Размер сборки вырос с ~5 МБ до ~200 МБ — Дмитрий заметил; Aleksey: оптимизация ПОСЛЕ стабильной версии.
- План: добить P1 → отдать версию на тестирование → параллельно AI-часть.
