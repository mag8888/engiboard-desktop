{
  "name": "engiboard-desktop",
  "version": "0.1.81",
  "description": "EngiBoard Desktop – test runner",
  "private": true,
  "scripts": {
    "start": "npx serve -s dist -p ${PORT:-3000}",
    "sync-dist": "cp index.html dist/index.html && cp editor.html dist/editor.html && cp sniper.html dist/sniper.html 2>/dev/null; echo 'dist synced'",
    "prebuild": "npm run sync-dist",
    "build": "npm run sync-dist && cargo tauri build",
    "test:unit": "node tests/unit.test.js",
    "test:e2e": "npx playwright test tests/e2e.test.js --reporter=list",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
